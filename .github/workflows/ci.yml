name: Python application

on:
  push:
    branches: [leehangjoo-patch]
  pull_request:
    branches: [leehangjoo-patch]

jobs:
  build:
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: "3.8"
    - name: Display Python version
      run: python -c "import sys; print(sys.version)
